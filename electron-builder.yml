appId: com.inakb.novel
productName: Novel InaKB
directories:
  output: release
asar: false
files:
  - dist/**/*
  - dist-electron/**/*
  - public/**/*
  - package.json
  # 主进程只用 electron/fs/path 内置模块，不需要 node_modules
win:
  target: [dir]
mac:
  target: [dmg, zip]
  icon: public/favicon.svg
  category: public.app-category.productivity
linux:
  target: [AppImage, deb]
  icon: public/favicon.svg
  category: Office
